import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    paddingRight: 8,
    paddingLeft: 8,
    paddingBottom: 8,
    borderBottomWidth: 1,
    borderColor: "#000000"
  }
});

export default styles;

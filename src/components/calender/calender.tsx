import React, { ReactElement } from "react";
import { View, Text } from "react-native";
import styles from "./calender.styles";

export default function Calender(): ReactElement {
  return (
    <View>
      <Text>Hello, this is calender component</Text>
    </View>
  );
}

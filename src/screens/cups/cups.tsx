import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Cups = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>This is Cup page.</Text>
    </View>
  );
};

export default Cups;

const styles = StyleSheet.create({});
